name: Complete Deployment Pipeline

on:
  push:
    branches: [ main ]
    paths:
      - 'ai-backend/**'
  workflow_dispatch:

jobs:
  setup-server:
    name: Setup Server Environment
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
       
    - name: Verify repository structure
      run: |
        echo "🔍 Repository structure:"
        ls -la
        echo "🔍 AI Backend directory contents:"
        ls -la ai-backend/

    - name: Check if server is already set up
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: 22
        script: |
          echo "🔍 Checking if server is already set up..."
          
          # Check if Node.js, PM2, and Caddy are installed
          if command -v node &> /dev/null && command -v npm &> /dev/null && command -v caddy &> /dev/null; then
            echo "✅ Server is already set up!"
            echo "Node.js: $(node --version)"
            echo "npm: $(npm --version)"
            echo "Caddy: $(caddy version)"
            
            # Check if directories exist
            if [ -d "/opt/ai-backend" ] && [ -d "/var/log/ai-backend" ]; then
              echo "✅ Required directories already exist"
              echo "✅ Server setup complete - skipping installation"
              exit 0
            else
              echo "⚠️ Software installed but directories missing - creating them"
              sudo mkdir -p /opt/ai-backend /var/log/ai-backend
              sudo chown -R $USER:$USER /opt/ai-backend /var/log/ai-backend
              echo "✅ Directories created - server setup complete"
              exit 0
            fi
          else
            echo "❌ Server not set up - proceeding with installation"
            exit 1
          fi

    - name: Upload and run installation script
      if: failure()
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: 22
        source: "install-server.sh"
        target: "/home/${{ secrets.SERVER_USER }}/install-server.sh"

    - name: Run server setup script
      if: failure()
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: 22
        script: |
          chmod +x ~/install-server.sh
          ~/install-server.sh ai ai.ekowlabs.space 5002

           # Create app folder
           sudo mkdir -p /opt/ai-backend /var/log/ai-backend
           sudo chown -R $USER:$USER /opt/ai-backend /var/log/ai-backend

  deploy-app:
    name: Deploy AI Backend Application
    runs-on: ubuntu-latest
    needs: setup-server
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install dependencies locally
      run: |
        cd ai-backend
        npm ci

    - name: Create deployment package
      run: |
        mkdir -p deploy
        cp -r ai-backend/src deploy/

        cp ai-backend/package*.json deploy/

        # Create .env file
        cat > deploy/.env << EOF
        MONGODB_URI=${{ secrets.MONGODB_URI }}
        OPEN_ROUTER=${{ secrets.OPEN_ROUTER }}
        PORT=5002
        NODE_ENV=production
        EOF

        tar -czf ai-backend.tar.gz -C deploy .

    - name: Upload deployment package
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: 22
        source: "ai-backend.tar.gz"
        target: "/opt/ai-backend/"

    - name: Extract and configure systemd service
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: 22
        script: |
          cd /opt/ai-backend
          tar -xzf ai-backend.tar.gz
          rm ai-backend.tar.gz

          # Create systemd service
          cat | sudo tee /etc/systemd/system/ai-backend.service << 'EOF'
          [Unit]
          Description=AI Backend Node.js Server
          After=network.target

          [Service]
          ExecStart=/usr/bin/node /opt/ai-backend/src/server.js
          Restart=always
          User=${{ secrets.SERVER_USER }}
          Environment=NODE_ENV=production
          Environment=PORT=5002
          Environment=MONGODB_URI=${{ secrets.MONGODB_URI }}
          Environment=OPEN_ROUTER=${{ secrets.OPEN_ROUTER }}
          WorkingDirectory=/opt/ai-backend
          StandardOutput=file:/var/log/ai-backend/out.log
          StandardError=file:/var/log/ai-backend/error.log

          [Install]
          WantedBy=multi-user.target
          EOF

          # Enable and start service
          sudo systemctl daemon-reload
          sudo systemctl enable ai-backend
          sudo systemctl restart ai-backend
          sudo systemctl status ai-backend --no-pager

    - name: Health check
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: 22
        script: |
          sleep 10
          if systemctl is-active --quiet ai-backend; then
            echo "✅ Application is running successfully!"
          else
            echo "❌ Application failed to start"
            sudo journalctl -u ai-backend --no-pager -n 50
            exit 1
          fi
          if curl -f -s http://localhost:5002/ > /dev/null; then
            echo "✅ API is responding"
          else
            echo "⚠️ API not responding yet"
          fi
